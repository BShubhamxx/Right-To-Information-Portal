import { SiteHeader } from "@/components/site-header";
import { KnowledgeLibrary } from "@/components/knowledge-library";
export default async function KnowledgePage({ searchParams }: { searchParams: Promise<{ query?: string }> }) { const params = await searchParams; return <main className="min-h-screen bg-canvas"><SiteHeader /><div className="mx-auto max-w-5xl px-4 py-8 lg:px-6"><p className="text-sm text-slate-600">Help / Illustrative examples</p><h1 className="mt-1 text-3xl font-bold text-[#123B52]">RTI Knowledge Library</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-700">Explore patterns that can help you ask for specific records. These examples are fictional and should be edited for your situation.</p><KnowledgeLibrary initialQuery={params.query} /></div></main>; }
