import { SiteHeader } from "@/components/site-header";
import { QuestionDecomposition } from "@/components/question-decomposition";
export default async function DecomposePage({ searchParams }: { searchParams: Promise<{ question?: string }> }) { const params = await searchParams; return <main className="min-h-screen bg-canvas"><SiteHeader /><div className="mx-auto max-w-4xl px-4 py-8 lg:px-6"><p className="text-sm text-slate-600">File an RTI / Review question</p><h1 className="mt-1 text-3xl font-bold text-[#123B52]">Break your question into parts</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-700">When a question asks for several kinds of information, reviewing the groups can make the final request clearer. You decide what to keep together.</p><QuestionDecomposition initialQuestion={params.question} /></div></main>; }
