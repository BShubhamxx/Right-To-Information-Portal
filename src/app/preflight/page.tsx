import { SiteHeader } from "@/components/site-header";
import { PreflightCheck } from "@/components/preflight-check";
export default async function PreflightPage({ searchParams }: { searchParams: Promise<{ question?: string }> }) { const params = await searchParams; const question = params.question || "How much money was spent on road repairs in Pune in 2025?"; return <main className="min-h-screen bg-canvas"><SiteHeader /><div className="mx-auto max-w-4xl px-4 py-8 lg:px-6"><p className="text-sm text-slate-600">File an RTI / Final check</p><h1 className="mt-1 text-3xl font-bold text-[#123B52]">Can I actually file this?</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-700">Bring together the route, request wording, context, eligibility and document checks before entering the application form.</p><PreflightCheck question={question} /></div></main>; }
