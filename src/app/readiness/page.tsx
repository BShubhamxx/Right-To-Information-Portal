import { SiteHeader } from "@/components/site-header";
import { ReadinessCheck } from "@/components/readiness-check";
export default async function ReadinessPage({ searchParams }: { searchParams: Promise<{ question?: string }> }) { const params = await searchParams; const question = params.question || "How much money was spent on road repairs in Pune in 2025?"; return <main className="min-h-screen bg-canvas"><SiteHeader /><div className="mx-auto max-w-4xl px-4 py-8 lg:px-6"><p className="text-sm text-slate-600">File an RTI / Improve request</p><h1 className="mt-1 text-3xl font-bold text-[#123B52]">Is your RTI request ready?</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-700">Review the request for missing context and clearer information types before you continue to the existing filing steps.</p><div className="service-panel mt-6 bg-slate-50 p-4 text-sm leading-6 text-slate-700"><strong>Your request:</strong> {question}</div><ReadinessCheck question={question} /></div></main>; }
