export type KnowledgeItem = { title: string; category: string; topic: string; informationType: string; exampleQuestion: string; exampleRequest: string; authorityCategory: string; keywords: string[]; explanation: string; demoOnly: true };
export function findRelatedKnowledge(question: string, items: KnowledgeItem[]) { const terms = question.toLowerCase().split(/\W+/).filter((term) => term.length > 2); return items.map((item) => ({ item, score: terms.filter((term) => `${item.title} ${item.topic} ${item.keywords.join(" ")}`.toLowerCase().includes(term)).length })).filter((entry) => entry.score > 0).sort((a, b) => b.score - a.score).slice(0, 5).map((entry) => entry.item); }
